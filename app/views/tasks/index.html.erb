<p>Now is <%= Time.now %><p>

  <ul>
    <% @tasks.each do |task| %>
    <li>
     <% if task.completed == true %>
     <%= "[x]" %>
     <p> <%= link_to task.name, task_path(task[:id]) %></p>
     <% else %>
     <%= "[ ]" %>
     <p> <%= link_to task.name, task_path(task[:id]) %></p>
     <% end %>
     <p><%= link_to "🗑", task_path(task[:id]), method: :delete %></p>
   </ul>
   <% end %>
   <%= link_to "+ Add a new task", new_task_path %>
 </ul>
